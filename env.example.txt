# ================================
# SECURITY CRITICAL - REQUIRED
# ================================

# Authentication Secret (REQUIRED)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# Encryption Keys (REQUIRED - NO DEFAULTS)
# Generate with: openssl rand -hex 32
OAUTH_ENCRYPTION_KEY="your-64-char-hex-key-here"
MFA_ENCRYPTION_KEY="your-64-char-hex-key-here"

# MFA Trusted Device Settings (Optional - defaults shown)
MFA_TRUST_DURATION_DAYS="60"  # How long trusted devices remain valid
MFA_MAX_TRUSTED_DEVICES_PER_USER="5"  # Max number of trusted devices per user
MFA_APP_SCOPE="app.yourdomain.com"  # Optional but recommended to keep MFA scope stable across deploys
MFA_COOKIE_DOMAIN="yourdomain.com"  # Optional but recommended for cookie persistence across subdomains

# Company deletion safety (recommended)
# Keep these false/missing in production to fully disable company deletion.
ALLOW_PROD_COMPANY_DELETE="false"
NEXT_PUBLIC_ALLOW_PROD_COMPANY_DELETE="false"
CRON_SECRET="your-random-secret-for-cron-endpoints"  # For securing cron endpoints

# ================================
# DATABASE
# ================================

# PostgreSQL Connection String
DATABASE_URL="postgresql://user:password@host:5432/database"

# ================================
# PAYMENT PROCESSING (USAePay)
# ================================

USAEPAY_API_KEY="your-usaepay-api-key"
USAEPAY_PIN="your-usaepay-pin"
USAEPAY_SOURCE_KEY="your-usaepay-source-key"
USAEPAY_SANDBOX="true"  # Set to false in production
USAEPAY_WEBHOOK_SECRET="your-webhook-secret"

# ================================
# QUICKBOOKS INTEGRATION
# ================================

QUICKBOOKS_CLIENT_ID="your-quickbooks-client-id"
QUICKBOOKS_CLIENT_SECRET="your-quickbooks-client-secret"
QUICKBOOKS_ENVIRONMENT="sandbox"  # or "production"
QUICKBOOKS_REDIRECT_URI="http://localhost:3000/api/quickbooks/callback"

# ================================
# XERO INTEGRATION
# ================================

XERO_CLIENT_ID="your-xero-client-id"
XERO_CLIENT_SECRET="your-xero-client-secret"
XERO_REDIRECT_URI="http://localhost:3000/api/xero/callback"
XERO_SCOPES="accounting.transactions.read accounting.reports.read accounting.contacts.read accounting.settings.read offline_access"

# ================================
# EMAIL (Resend)
# ================================

RESEND_API_KEY="your-resend-api-key"

# ================================
# AI/OpenAI (Optional)
# ================================

OPENAI_API_KEY="your-openai-api-key"

# Optional override
OPENAI_MODEL="gpt-4o"

# Web search provider key for AI Analysis (AI Search)
# This implementation uses SerpApi: https://serpapi.com/
SERPAPI_API_KEY="your-serpapi-api-key"

# ================================
# NODE ENVIRONMENT
# ================================

NODE_ENV="development"  # or "production"

# ================================
# VERCEL (Production Only)
# ================================

# These are automatically set by Vercel:
# VERCEL="1"
# VERCEL_ENV="production"  # or "preview", "development"
# VERCEL_URL="your-app.vercel.app"

